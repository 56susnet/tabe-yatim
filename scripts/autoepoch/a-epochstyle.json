{
  "default": {
    "train_batch_size": 4,
    "max_train_epochs": 10,
    "save_every_n_epochs": 2
  },
  "size_ranges": [
    {
      "min": 1,
      "max": 20,
      "config": {
        "max_train_epochs": 56,
        "save_every_n_epochs": 28
      }
    },
    {
      "min": 21,
      "max": 40,
      "config": {
        "max_train_epochs": 45,
        "save_every_n_epochs": 22
      }
    },
    {
      "min": 41,
      "max": 60,
      "config": {
        "max_train_epochs": 38,
        "save_every_n_epochs": 19
      }
    }
  ]
}